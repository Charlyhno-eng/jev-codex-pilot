# Codex models available to JEV. The keys are stable routing names shown in the app;
# the values are the model IDs passed to Codex. Keep models ordered from lighter to stronger.
[models]
luna = "gpt-6-luna"
sol = "gpt-6-sol"
astra = "gpt-6-astra"

# Reasoning choices are ordered from lowest to highest effort.
[reasoning]
levels = ["low", "medium", "high", "xhigh", "max"]

# Each level lists permitted model:effort pairs. The first pair is JEV's default.
# Edit model IDs above, then these routes when models or recommendations change.
[complexity.0]
routes = ["luna:medium"]

[complexity.1]
routes = ["luna:high"]

[complexity.2]
routes = ["luna:xhigh", "luna:max"]

[complexity.3]
routes = ["sol:high", "sol:xhigh"]

[complexity.4]
routes = ["sol:xhigh", "astra:low", "astra:medium"]

[complexity.5]
routes = ["astra:high"]
